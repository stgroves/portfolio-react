# portfolio-react
A self‑editing portfolio website powered by a custom JSON‑driven UI engine.

The site renders itself from a declarative JSON tree and includes an in‑browser editor that allows pages, layouts, and content to be created and modified directly from the site.

The engine features a node resolution pipeline, modifier system, event bus, and node‑owned render functions, enabling dynamic UI generation without hardcoded pages.